---
import Link from '../atoms/Link.astro';

/**
 * Definiert die Eigenschaften für die EmailLink-Komponente.
 */
interface Props {
    /** Die E-Mail-Adresse, die verlinkt und angezeigt wird. */
    email: string;
}

const { email } = Astro.props;
---

<p>📧 E-Mail: <Link href={`mailto:${email}`}>{email}</Link></p>