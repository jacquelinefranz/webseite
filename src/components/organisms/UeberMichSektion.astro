---
import { getEntryBySlug } from 'astro:content';
import Ueberschrift2 from '../atoms/Ueberschrift2.astro'; // Keep if you want consistent styling for headline
// Absatz import is not strictly needed if Content renders paragraphs, but can be kept for other reasons.

const ueberMichEntry = await getEntryBySlug('sections', 'ueber-mich');
const { Content } = await ueberMichEntry.render();
---

<section>
    <Ueberschrift2 inhalt={ueberMichEntry.data.title} />
    <Content />
</section>
