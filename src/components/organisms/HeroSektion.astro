---
import ProfilBild from '../atoms/ProfilBild.astro';
---

<section class="hero-card">
    <div class="hero-body">
        <ProfilBild />
        
        <hgroup class="hero-content">
            <h2>Hi, ich bin Jacqueline Franz</h2>
            <p class="role">Product Owner | <span class="handle">@digitalerPanda 🐼</span></p>
            
            <p class="description">
                Ich lerne öffentlich, experimentiere mit Code und teile dabei alles was interessant ist - Erfolge, Fehler und alles dazwischen.
            </p>
        </hgroup>
    </div>
</section>

<style>
    .hero-card {
        max-width: var(--seitenbreite-max);
        margin: 0 auto var(--abstand-sektion-vertikal) auto;
        overflow: hidden;
    }

    .hero-body {
        display: flex;
        flex-wrap: wrap;
        gap: var(--abstand-xl);
        align-items: center
    }

    .hero-content {
        flex: 1 1 20rem;
        display: flex;
        flex-direction: column;
        gap: var(--abstand-s);
        min-width: min(100%, 20rem);
    }
    
    .hero-content h2 {
        margin-top: 0;
    }
    
    .role {
        margin: 0;
        font-size: var(--schriftgroesse-m);
        font-weight: var(--schriftgewicht-semi-bold);
    }

    .handle {
        color: var(--sekundärfarbe);
    }

    .description {
        margin-top: var(--abstand-s);
        margin-bottom: 0;
    }
</style>
