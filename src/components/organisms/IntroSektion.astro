---
import { getEntryBySlug } from 'astro:content';
import Ueberschrift1 from '../atoms/Ueberschrift1.astro'; // Keep if you want consistent styling for headline
// Absatz import is not strictly needed if Content renders paragraphs, but can be kept for other reasons.
// For now, let's keep it simple and rely on Content.

const introEntry = await getEntryBySlug('sections', 'intro');
const { Content } = await introEntry.render();
---

<section>
    <Ueberschrift1 inhalt={introEntry.data.title} />
    <Content />
</section>
