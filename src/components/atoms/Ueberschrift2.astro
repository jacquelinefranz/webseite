---
/**
 * Definiert die Eigenschaften für die H2-Überschrift-Komponente.
 */
interface Props {
    // Props könnten hier definiert werden, falls benötigt. Aktuell werden nur Slots verwendet.
}
---

<h2><slot /></h2>
