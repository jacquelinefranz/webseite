---
import BasisLayout from '../components/templates/BasisLayout.astro';
import '../styles/tokens/variant-3.css'; // Nutze Variant 3 als Basis für die Übersicht
---

<BasisLayout
  seoTitle="Design-Varianten Übersicht - Jacqueline Franz"
  seoDescription="Vergleiche 3 verschiedene Design-Ansätze für mein Portfolio: Warm & Hands-on, Bold & Kreativ, Modern & Tech-Forward."
  seoType="website"
>
  <main class="variants-overview">
    <section class="intro">
      <h1>Design-Varianten Übersicht</h1>
      <p class="subtitle">
        Drei unterschiedliche visuelle Identitäten – eine Story. Entdecke verschiedene
        Design-Ansätze, die meine Journey von der Werkstatt ins Digitale erzählen.
      </p>
    </section>

    <div class="variants-grid">
      <!-- Variante 1: Warm & Hands-on -->
      <article class="variant-card variant-1">
        <div class="card-header">
          <span class="variant-label">Variante 1</span>
          <h2>Warm & Hands-on</h2>
        </div>
        <div class="card-content">
          <p class="description">
            Authentisch und handwerklich geprägt. Erdige, warme Töne (Terra, Kupfer, Sand)
            erzählen von der Brücke zwischen Werkstatt und Digital.
          </p>
          <div class="color-palette">
            <div class="color-swatch" style="background-color: #c2693d;" title="Primary: Kupfer"></div>
            <div class="color-swatch" style="background-color: #faf8f5;" title="Background: Warmes Weiß"></div>
            <div class="color-swatch" style="background-color: #d4a574;" title="Accent: Sand"></div>
          </div>
          <div class="keywords">
            <span class="keyword">Authentizität</span>
            <span class="keyword">Handwerk</span>
            <span class="keyword">Vertrauen</span>
          </div>
          <a href="/variant-1" class="cta-link">Variante ansehen →</a>
        </div>
      </article>

      <!-- Variante 2: Bold & Kreativ -->
      <article class="variant-card variant-2">
        <div class="card-header">
          <span class="variant-label">Variante 2</span>
          <h2>Bold & Kreativ</h2>
        </div>
        <div class="card-content">
          <p class="description">
            Mutig und kontrastreich. Lebendige Farben (Violet, Magenta, Petrol) zeigen die
            unkonventionelle Journey und den Mut zum Quereinstieg.
          </p>
          <div class="color-palette">
            <div class="color-swatch" style="background-color: #b83d9e;" title="Primary: Magenta"></div>
            <div class="color-swatch" style="background-color: #0f0a0d;" title="Background: Dunkel"></div>
            <div class="color-swatch" style="background-color: #2d9d9c;" title="Accent: Petrol"></div>
          </div>
          <div class="keywords">
            <span class="keyword">Mut</span>
            <span class="keyword">Kreativität</span>
            <span class="keyword">Kontrast</span>
          </div>
          <a href="/variant-2" class="cta-link">Variante ansehen →</a>
        </div>
      </article>

      <!-- Variante 3: Modern & Tech-Forward -->
      <article class="variant-card variant-3">
        <div class="card-header">
          <span class="variant-label">Variante 3</span>
          <h2>Modern & Tech-Forward</h2>
        </div>
        <div class="card-content">
          <p class="description">
            Systematisch und innovativ. Kühle technische Farben (Teal, Indigo, Slate)
            repräsentieren den Product Owner mit UX-Fokus und Zukunftsorientierung.
          </p>
          <div class="color-palette">
            <div class="color-swatch" style="background-color: #14b8a6;" title="Primary: Teal"></div>
            <div class="color-swatch" style="background-color: #f8fafc;" title="Background: Kühles Weiß"></div>
            <div class="color-swatch" style="background-color: #6366f1;" title="Accent: Indigo"></div>
          </div>
          <div class="keywords">
            <span class="keyword">Systematik</span>
            <span class="keyword">Innovation</span>
            <span class="keyword">Präzision</span>
          </div>
          <a href="/variant-3" class="cta-link">Variante ansehen →</a>
        </div>
      </article>
    </div>

    <section class="info-section">
      <h3>Über die Varianten</h3>
      <p>
        Alle drei Varianten nutzen dieselbe <strong>Atomic Design Architektur</strong> und
        <strong>Design Token System</strong>, unterscheiden sich aber in Farbpalette,
        Stimmung und visueller Persönlichkeit.
      </p>
      <ul>
        <li><strong>Shared Structure:</strong> Alle Komponenten funktionieren mit allen Token-Sets</li>
        <li><strong>Fluid Responsive:</strong> clamp() für Typography & Spacing ohne harte Breakpoints</li>
        <li><strong>Motion Design:</strong> Subtile Animationen und Micro-Interactions</li>
      </ul>
    </section>
  </main>
</BasisLayout>

<style>
  .variants-overview {
    max-width: 1200px;
    margin: 0 auto;
    padding: var(--spacing-xl) var(--container-padding);
  }

  .intro {
    text-align: center;
    margin-bottom: var(--spacing-3xl);
  }

  .intro h1 {
    font-size: var(--font-size-h1);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-md);
  }

  .subtitle {
    font-size: var(--font-size-md);
    color: var(--color-text-secondary);
    max-width: 700px;
    margin: 0 auto;
    line-height: var(--line-height-relaxed);
  }

  .variants-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--spacing-xl);
    margin-bottom: var(--spacing-3xl);
  }

  @media (min-width: 768px) {
    .variants-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .variants-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .variant-card {
    background: var(--color-background-surface);
    border: var(--border-width-thin) solid var(--color-border);
    border-radius: var(--border-radius-lg);
    padding: var(--spacing-xl);
    transition: var(--transition-all);
    box-shadow: var(--shadow-sm);
  }

  .variant-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .card-header {
    margin-bottom: var(--spacing-lg);
  }

  .variant-label {
    display: inline-block;
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-subtle);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--spacing-xs);
  }

  .card-header h2 {
    font-size: var(--font-size-h3);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin: 0;
  }

  .description {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-normal);
    margin-bottom: var(--spacing-lg);
  }

  .color-palette {
    display: flex;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-lg);
  }

  .color-swatch {
    width: 48px;
    height: 48px;
    border-radius: var(--border-radius-md);
    border: var(--border-width-thin) solid var(--color-border);
    box-shadow: var(--shadow-sm);
  }

  .keywords {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-xs);
    margin-bottom: var(--spacing-lg);
  }

  .keyword {
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-primary);
    background: var(--color-background-elevated);
    padding: var(--spacing-xs) var(--spacing-sm);
    border-radius: var(--border-radius-full);
    border: var(--border-width-thin) solid var(--color-border);
  }

  .cta-link {
    display: inline-flex;
    align-items: center;
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-semibold);
    color: var(--color-primary);
    text-decoration: none;
    transition: var(--transition-color);
  }

  .cta-link:hover {
    color: var(--color-primary-hover);
  }

  .info-section {
    background: var(--color-background-elevated);
    border-radius: var(--border-radius-lg);
    padding: var(--spacing-xl);
    border: var(--border-width-thin) solid var(--color-border);
  }

  .info-section h3 {
    font-size: var(--font-size-h3);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-md);
  }

  .info-section p {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-normal);
    margin-bottom: var(--spacing-md);
  }

  .info-section ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .info-section li {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    line-height: var(--line-height-normal);
    padding-left: var(--spacing-lg);
    position: relative;
    margin-bottom: var(--spacing-sm);
  }

  .info-section li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--color-primary);
    font-weight: var(--font-weight-bold);
  }
</style>
